using System;

namespace Example.GameStructures
{
    /// <summary>
    /// Declarations of some factions for mobiles to belong to.
    /// </summary>
    public static class Factions
    {
        /// <summary>
        /// Hostile characters (i.e. monsters).
        /// </summary>
        public const int GENERIC_HOSTILE = -1;

        /// <summary>
        /// Friendly characters (i.e. NPCs and players).
        /// </summary>
        public const int GENERIC_FRIENDLY = 0;
    }
}
